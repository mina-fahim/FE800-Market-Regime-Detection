[behave]
# Default features path — all .feature files live under tests/tests_behave/features/
paths = tests/tests_behave/features

# Show nicely formatted output by default
format   = pretty
show_timings = false

# Tags available across all suites:
#   @portfolio, @client, @simulation, @utils_data, @utils_tab_results,
#   @formatting, @construction, @properties, @validation, @calculation,
#   @benchmark, @csv, @integration, @smoke, @edge_case
